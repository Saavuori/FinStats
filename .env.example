# Copy to .env for local development. finstats needs no secrets — it is built
# entirely on open Statistics Finland data, called directly from the browser.
#
# The backend only serves the embedded frontend build plus /api/version and
# /api/health. PORT sets the listen address (the Vite dev proxy targets 8081).
PORT=8081
